{
  "name": "wje-24-engineering-3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "npm run check --prefix backend && npm run check --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "all": "npm run check && npm run build"
  }
}
